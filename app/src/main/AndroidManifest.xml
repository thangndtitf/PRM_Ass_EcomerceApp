<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.assignmentprojectprm">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AssignmentProjectPRM"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".View.Activities.PaymentActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.AddAddressActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.AddressActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD6zOjTNFCdU2nfg9NmZ6fMwyHsgQ_Bkn0" />

        <activity
            android:name=".View.Activities.CartActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.ShowAllActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.DetailActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.OnboardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.Activities.RegisActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".View.Activities.MainActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>